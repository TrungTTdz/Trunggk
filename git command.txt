git init  // tạo folder     
git clone     // git clone lấy từ trên mạng về
git pull    // đồng bộ từ trên mạng về máy

git add và git add .    //thêm vào máy
git commit
git push        // từ máy lên mạng

git log        // trạng thái file
git log —decorate —graph —oneline
